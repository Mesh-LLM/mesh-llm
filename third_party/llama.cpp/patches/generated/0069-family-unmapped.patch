From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: Mesh-LLM CI <ci@mesh-llm.local>
Date: Thu, 1 Jan 1970 00:00:00 +0000
Subject: [PATCH] skippy: generate unmapped stage controls

Generated by tools/skippy-stage-rewriter from compiler-proven builder
shapes. Auxiliary sidecars and whole-model multi-domain graphs are proven
separately and remain outside the primary decoder stage interval.

---
diff --git a/src/models/arctic.cpp b/src/models/arctic.cpp
index 09ee0f752f0ebb34e42b57b551448998b0745688..d274041d29623d7b6859a58f95c44cd742b558b4 100644
--- a/src/models/arctic.cpp
+++ b/src/models/arctic.cpp
@@ -62,16 +62,23 @@ llama_model_arctic::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -107,7 +114,7 @@ llama_model_arctic::graph::graph(const llama_model & model, const llm_graph_para
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -159,7 +166,15 @@ llama_model_arctic::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/baichuan.cpp b/src/models/baichuan.cpp
index 585f3614174160bc547bcea00918abc5c407f33a..6c3ff60d70bc26099bb92bcbc226172c64471c4b 100644
--- a/src/models/baichuan.cpp
+++ b/src/models/baichuan.cpp
@@ -52,16 +52,23 @@ llama_model_baichuan::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = model.type == LLM_TYPE_7B ? build_inp_pos() : nullptr;
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         cur = build_norm(inpL,
@@ -103,7 +110,7 @@ llama_model_baichuan::graph::graph(const llama_model & model, const llm_graph_pa
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -134,7 +141,15 @@ llama_model_baichuan::graph::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/bailingmoe.cpp b/src/models/bailingmoe.cpp
index 9d1073ae148708849874b3d5d46027e5058da082..5b3f6fb07c88be721a1758fca14b68d071a8cb57 100644
--- a/src/models/bailingmoe.cpp
+++ b/src/models/bailingmoe.cpp
@@ -63,16 +63,23 @@ llama_model_bailingmoe::graph::graph(const llama_model & model, const llm_graph_
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -111,7 +118,7 @@ llama_model_bailingmoe::graph::graph(const llama_model & model, const llm_graph_
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_rot)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -159,7 +166,15 @@ llama_model_bailingmoe::graph::graph(const llama_model & model, const llm_graph_
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/bailingmoe2.cpp b/src/models/bailingmoe2.cpp
index 24fc4e0226b4d3380786a3e0428673373d016154..ab2e16e6e5522ffbc94151c52ac9f86609c25fd1 100644
--- a/src/models/bailingmoe2.cpp
+++ b/src/models/bailingmoe2.cpp
@@ -96,16 +96,23 @@ llama_model_bailingmoe2::graph::graph(const llama_model & model, const llm_graph
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -138,7 +145,7 @@ llama_model_bailingmoe2::graph::graph(const llama_model & model, const llm_graph
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -192,7 +199,15 @@ llama_model_bailingmoe2::graph::graph(const llama_model & model, const llm_graph
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/bert.cpp b/src/models/bert.cpp
index ca0281d306b157d4d1e0f1d882ee6f986b4f1ec5..093984fa0f76d063d9f556d156290f671d5d28b6 100644
--- a/src/models/bert.cpp
+++ b/src/models/bert.cpp
@@ -91,27 +91,34 @@ llama_model_bert::graph::graph(const llama_model & model, const llm_graph_params
     }
 
     // construct input embeddings (token, type, position)
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // token types are hardcoded to zero ("Sentence A")
-    if (model.type_embd) {
+    if ((!stage_filtered || il_start == 0) && (model.type_embd)) {
         ggml_tensor * type_row0 = ggml_view_1d(ctx0, model.type_embd, n_embd, 0);
         inpL                    = ggml_add(ctx0, inpL, type_row0);
     }
-    if (model.arch == LLM_ARCH_BERT) {
+    if ((!stage_filtered || il_start == 0) && (model.arch == LLM_ARCH_BERT)) {
         inpL = ggml_add(ctx0, ggml_get_rows(ctx0, model.pos_embd, inp_pos), inpL);
     }
     cb(inpL, "inp_embd", -1);
 
     // embed layer norm
-    inpL = build_norm(inpL, model.tok_norm, model.tok_norm_b, LLM_NORM, 0);
+    inpL = stage_filtered && il_start > 0 ? inpL : build_norm(inpL, model.tok_norm, model.tok_norm_b, LLM_NORM, 0);
     cb(inpL, "inp_norm", 0);
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * cur = inpL;
 
         {
@@ -154,7 +161,7 @@ llama_model_bert::graph::graph(const llama_model & model, const llm_graph_params
             cb(cur, "kqv_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -222,7 +229,15 @@ llama_model_bert::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/bitnet.cpp b/src/models/bitnet.cpp
index fcb4910e3f577704c7fc9c3fb6e3e127d613b49d..e643195e0a8e25d705d3028d274fd754125a0616 100644
--- a/src/models/bitnet.cpp
+++ b/src/models/bitnet.cpp
@@ -57,16 +57,23 @@ llama_model_bitnet::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         cur = build_norm(inpL,
@@ -111,7 +118,7 @@ llama_model_bitnet::graph::graph(const llama_model & model, const llm_graph_para
             cb(cur, "attn_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -149,7 +156,15 @@ llama_model_bitnet::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/codeshell.cpp b/src/models/codeshell.cpp
index de53bb9818479cefa2f6196d235db0988a59c720..281803ed546e31f807cb173244fa7401121a61b7 100644
--- a/src/models/codeshell.cpp
+++ b/src/models/codeshell.cpp
@@ -59,16 +59,23 @@ llama_model_codeshell::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         cur = build_norm(inpL,
                 model.layers[il].attn_norm,
                 model.layers[il].attn_norm_b,
@@ -101,7 +108,7 @@ llama_model_codeshell::graph::graph(const llama_model & model, const llm_graph_p
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -134,7 +141,15 @@ llama_model_codeshell::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = build_norm(inpL,
             model.output_norm,
diff --git a/src/models/cogvlm.cpp b/src/models/cogvlm.cpp
index 750f57a394e3ed6d0bf62b8833b644db17454376..7f48c9520a40f8ec6bae954e05db95109158ecc0 100644
--- a/src/models/cogvlm.cpp
+++ b/src/models/cogvlm.cpp
@@ -61,7 +61,12 @@ llama_model_cogvlm::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * inpL;
     ggml_tensor * cur;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
 
@@ -76,7 +81,9 @@ llama_model_cogvlm::graph::graph(const llama_model & model, const llm_graph_para
         is_text = false;
     }
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         // get either the text or image weight tensors
         ggml_tensor *wqkv, *wo, *wo_s;
         ggml_tensor *ffn_gate, *ffn_down, *ffn_up;
@@ -143,7 +150,15 @@ llama_model_cogvlm::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/cohere2moe.cpp b/src/models/cohere2moe.cpp
index 5e02cd56e71d2a63fb2b34f9aa78fb3ed5576f5b..2bf25c593c3439028d89858167a8c0f93d81ad0f 100644
--- a/src/models/cohere2moe.cpp
+++ b/src/models/cohere2moe.cpp
@@ -163,14 +163,21 @@ llama_model_cohere2moe::graph::graph(const llama_model & model, const llm_graph_
     const llm_norm_type cohere2moe_norm_type = hparams.f_norm_rms_eps == 0.0f ? LLM_NORM : LLM_NORM_RMS;
     const float f_logit_scale = hparams.f_logit_scale;
     ggml_tensor * cur;
-    ggml_tensor * inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv_iswa();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     // MTP/NextN layers are loaded as extra decoder blocks but not executed in the main pass.
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         const bool is_swa = hparams.is_swa(il);
         // Dense-prefix full-attention layers use RoPE; later layers follow the SWA pattern.
         const bool force_rope = static_cast<uint32_t>(il) < hparams.n_layer_dense_lead;
@@ -210,7 +217,7 @@ llama_model_cohere2moe::graph::graph(const llama_model & model, const llm_graph_
                     1.0f / sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids && cparams.embeddings_nextn_masked) {
+        if (il == il_end - 1 && inp_out_ids && cparams.embeddings_nextn_masked) {
             cur     = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpL    = ggml_get_rows(ctx0, inpL, inp_out_ids);
             ffn_inp = ggml_get_rows(ctx0, ffn_inp, inp_out_ids);
@@ -266,7 +273,15 @@ llama_model_cohere2moe::graph::graph(const llama_model & model, const llm_graph_
         cb(cur, "l_out", il);
 
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
     cur = build_norm(cur, model.output_norm, nullptr, cohere2moe_norm_type, -1);
diff --git a/src/models/command-r.cpp b/src/models/command-r.cpp
index 94a46188bb89698b40f39ebb7f829ae5a1a4babe..95ffaabc8e19d7768f92ed8656aae7e891e9b698 100644
--- a/src/models/command-r.cpp
+++ b/src/models/command-r.cpp
@@ -54,16 +54,23 @@ llama_model_command_r::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         // norm
         cur = build_norm(inpL, model.layers[il].attn_norm, NULL, LLM_NORM, il);
         cb(cur, "attn_norm", il);
@@ -98,7 +105,7 @@ llama_model_command_r::graph::graph(const llama_model & model, const llm_graph_p
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur     = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpL    = ggml_get_rows(ctx0, inpL, inp_out_ids);
             ffn_inp = ggml_get_rows(ctx0, ffn_inp, inp_out_ids);
@@ -123,7 +130,15 @@ llama_model_command_r::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM, -1);
diff --git a/src/models/dbrx.cpp b/src/models/dbrx.cpp
index 4f5ac4d06a4542a3dc37b20095c18677c1392aae..8c89b9869856a6e2ceda679d7c2f8b1de9860099 100644
--- a/src/models/dbrx.cpp
+++ b/src/models/dbrx.cpp
@@ -53,16 +53,23 @@ llama_model_dbrx::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -97,7 +104,7 @@ llama_model_dbrx::graph::graph(const llama_model & model, const llm_graph_params
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -133,7 +140,15 @@ llama_model_dbrx::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/deci.cpp b/src/models/deci.cpp
index cdfcf29e02f57e6d384f6ce990f39d52fb999cb8..1c353c15375bccf2d9036d19f6efac64141342ad 100644
--- a/src/models/deci.cpp
+++ b/src/models/deci.cpp
@@ -86,7 +86,12 @@ llama_model_deci::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -96,9 +101,11 @@ llama_model_deci::graph::graph(const llama_model & model, const llm_graph_params
     const float kq_scale =
         hparams.f_attention_scale == 0.0f ? 1.0f / sqrtf(float(n_embd_head)) : hparams.f_attention_scale;
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA     = inpL;
         const int64_t n_head_kv = hparams.n_head_kv(il);
         const int64_t n_head    = hparams.n_head(il);
@@ -139,12 +146,13 @@ llama_model_deci::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
         // FFN-free layer of Llama-3_1-Nemotron-Ultra-253B
         if (n_ff == 0) {
+            end_block(inpL, il);
             continue;
         }
         // modified to support attention-free layer of Llama-3_1-Nemotron-51B
@@ -173,7 +181,15 @@ llama_model_deci::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/deepseek.cpp b/src/models/deepseek.cpp
index a47a9c3dafcafb1ca62ed749ed979caa6322cca1..e441b3506dff18ceed7a710c11f6279604387227 100644
--- a/src/models/deepseek.cpp
+++ b/src/models/deepseek.cpp
@@ -81,7 +81,12 @@ llama_model_deepseek::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -91,9 +96,11 @@ llama_model_deepseek::graph::graph(const llama_model & model, const llm_graph_pa
     const float kq_scale =
         hparams.f_attention_scale == 0.0f ? 1.0f / sqrtf(float(n_embd_head)) : hparams.f_attention_scale;
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -123,7 +130,7 @@ llama_model_deepseek::graph::graph(const llama_model & model, const llm_graph_pa
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -176,7 +183,15 @@ llama_model_deepseek::graph::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/deepseek4.cpp b/src/models/deepseek4.cpp
index 6bf9d34449426504cea436531118eb9b13941f20..8efb9a7eef8e929cabbcb1e99d10c0a4a42d2900 100644
--- a/src/models/deepseek4.cpp
+++ b/src/models/deepseek4.cpp
@@ -1224,19 +1224,35 @@ llama_model_deepseek4::graph::graph(const llama_model & model, const llm_graph_p
     llm_graph_context(params) {
     ggml_tensor * cur;
 
-    ggml_tensor * inp = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * inp = (!stage_filtered || il_start == 0) ? build_inp_embd(model.tok_embd) : nullptr;
     ggml_tensor * inp_pos = build_inp_pos();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
     llm_graph_input_dsv4 * inp_dsv4 = build_inp_dsv4();
     llm_graph_input_dsv4_raw * inp_attn = inp_dsv4->get_raw();
     ggml_build_forward_expand(gf, inp_attn->self_kq_mask);
 
     const int64_t hc = hparams.dsv4_hc_mult;
-    ggml_tensor * inpL = ggml_reshape_3d(ctx0, inp, n_embd, 1, n_tokens);
-    inpL = ggml_repeat_4d(ctx0, inpL, n_embd, hc, n_tokens, 1);
+    ggml_tensor * inpL = stage_filtered && il_start > 0 ? nullptr : ggml_reshape_3d(ctx0, inp, n_embd, 1, n_tokens);
+    if (stage_filtered && il_start > 0) {
+        auto stage_inp = std::make_unique<llm_graph_input_hyperconnection>(n_embd, hc);
+        stage_inp->values = ggml_new_tensor_3d(ctx0, GGML_TYPE_F32, n_embd, hc, n_tokens);
+        cb(stage_inp->values, "hc_stage_input", -1);
+        ggml_set_input(stage_inp->values);
+        inpL = stage_inp->values;
+        res->t_skippy_activation_input = inpL;
+        res->add_input(std::move(stage_inp));
+    }
+    inpL = stage_filtered && il_start > 0 ? inpL : ggml_repeat_4d(ctx0, inpL, n_embd, hc, n_tokens, 1);
     cb(inpL, "hc_init", -1);
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         if ((size_t) il < cparams.embeddings_layer_inp.size() && cparams.embeddings_layer_inp[il]) {
             res->t_layer_inp[il] = dsv4_hc_mean(ctx0, inpL);
             cb(res->t_layer_inp[il], "layer_inp", il);
@@ -1324,8 +1340,17 @@ llama_model_deepseek4::graph::graph(const llama_model & model, const llm_graph_p
         inpL = build_hc_post(cur, residual, post, comb, il);
         inpL = build_cvec(inpL, il);
         cb(inpL, "l_last", il);
+        end_block(inpL, il);
+    }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_skippy_activation_output = inpL;
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
     }
 
+
     if ((size_t) n_layer < cparams.embeddings_layer_inp.size() && cparams.embeddings_layer_inp[n_layer]) {
         res->t_layer_inp[n_layer] = dsv4_hc_mean(ctx0, inpL);
         cb(res->t_layer_inp[n_layer], "layer_inp", n_layer);
diff --git a/src/models/dots3note.cpp b/src/models/dots3note.cpp
index 0991c488e468e61f65964a62e8833e9c64a5ab0c..59085c511b43180cdc09b801d0881b921c5b2547 100644
--- a/src/models/dots3note.cpp
+++ b/src/models/dots3note.cpp
@@ -165,15 +165,22 @@ llama_model_dots3note::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
 
     llm_graph_input_attn_k_dsa_iswa * inp_attn = build_attn_inp_k_dsa_iswa();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         const bool is_swa = hparams.is_swa(il);
@@ -403,7 +410,7 @@ llama_model_dots3note::graph::graph(const llama_model & model, const llm_graph_p
             }
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -458,7 +465,15 @@ llama_model_dots3note::graph::graph(const llama_model & model, const llm_graph_p
         cb(cur, "l_out", il);
 
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/dream.cpp b/src/models/dream.cpp
index abe737c335a4a377c6c44663c82c698d041f0140..31d362cf4817d662479a7c246117d739902fbe2b 100644
--- a/src/models/dream.cpp
+++ b/src/models/dream.cpp
@@ -60,16 +60,23 @@ llama_model_dream::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -95,7 +102,7 @@ llama_model_dream::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -120,7 +127,15 @@ llama_model_dream::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/eurobert.cpp b/src/models/eurobert.cpp
index 0948d7de656f1cac68c3ab48248c3bab4b1a302f..aa8b5604c1210dd1a9de8a485de5a035729ef469 100644
--- a/src/models/eurobert.cpp
+++ b/src/models/eurobert.cpp
@@ -44,14 +44,21 @@ llama_model_eurobert::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * inpL;
     ggml_tensor * inp_pos = build_inp_pos();
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     cb(inpL, "inp_embd", -1);
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * cur = inpL;
 
         cur = build_norm(inpL,
@@ -84,7 +91,7 @@ llama_model_eurobert::graph::graph(const llama_model & model, const llm_graph_pa
             cb(cur, "kqv_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -110,7 +117,15 @@ llama_model_eurobert::graph::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/exaone-moe.cpp b/src/models/exaone-moe.cpp
index 976ee050adcdbbf28790b0443732985cf28495bb..d66fc77bb4ed7d3f4b16b60be7fba180fabdee75 100644
--- a/src/models/exaone-moe.cpp
+++ b/src/models/exaone-moe.cpp
@@ -117,16 +117,23 @@ llama_model_exaone_moe::graph::graph(const llama_model & model, const llm_graph_
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn_iswa = build_attn_inp_kv_iswa();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // use RoPE for SWA layers
@@ -165,7 +172,7 @@ llama_model_exaone_moe::graph::graph(const llama_model & model, const llm_graph_
                 Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
             cb(cur, "attn_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -222,7 +229,15 @@ llama_model_exaone_moe::graph::graph(const llama_model & model, const llm_graph_
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     // final norm
diff --git a/src/models/exaone.cpp b/src/models/exaone.cpp
index 676fb37b5a63956e1ba2ed07a60288fedcf39eb0..92db9795a99475b85d88137db91b2669ff1cea81 100644
--- a/src/models/exaone.cpp
+++ b/src/models/exaone.cpp
@@ -53,16 +53,23 @@ llama_model_exaone::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -92,7 +99,7 @@ llama_model_exaone::graph::graph(const llama_model & model, const llm_graph_para
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -118,7 +125,15 @@ llama_model_exaone::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/gemma-embedding.cpp b/src/models/gemma-embedding.cpp
index 80ed3b1a4605d0e6839eab9e2cf6d0df5f1cd12d..864de59700d7a1417c254edae8d5943fb07070cb 100644
--- a/src/models/gemma-embedding.cpp
+++ b/src/models/gemma-embedding.cpp
@@ -79,10 +79,15 @@ llama_model_gemma_embedding::graph::graph(const llama_model & model, const llm_g
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // important: do not normalize weights for raw embeddings input (i.e. encoded image embeddings)
-    inpL = ggml_scale(ctx0, inpL, ubatch.token ? sqrtf(n_embd) : 1.0f);
+    inpL = stage_filtered && il_start > 0 ? inpL : ggml_scale(ctx0, inpL, ubatch.token ? sqrtf(n_embd) : 1.0f);
     cb(inpL, "inp_scaled", -1);
 
     // inp_pos - contains the positions
@@ -90,9 +95,11 @@ llama_model_gemma_embedding::graph::graph(const llama_model & model, const llm_g
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         const float freq_base_l  = model.get_rope_freq_base(cparams, il);
         const float freq_scale_l = model.get_rope_freq_scale(cparams, il);
 
@@ -131,7 +138,7 @@ llama_model_gemma_embedding::graph::graph(const llama_model & model, const llm_g
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f, il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -165,7 +172,15 @@ llama_model_gemma_embedding::graph::graph(const llama_model & model, const llm_g
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/gpt2.cpp b/src/models/gpt2.cpp
index 45afbccc1210f973038d4455c43a166d98312a3b..d1c331d393a80d117966930bf296cfcaab934852 100644
--- a/src/models/gpt2.cpp
+++ b/src/models/gpt2.cpp
@@ -64,7 +64,12 @@ llama_model_gpt2::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * pos;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -74,12 +79,14 @@ llama_model_gpt2::graph::graph(const llama_model & model, const llm_graph_params
     pos = ggml_get_rows(ctx0, model.pos_embd, inp_pos);
     cb(pos, "pos_embd", -1);
 
-    inpL = ggml_add(ctx0, inpL, pos);
+    inpL = stage_filtered && il_start > 0 ? inpL : ggml_add(ctx0, inpL, pos);
     cb(inpL, "inpL", -1);
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         cur = build_norm(inpL,
                 model.layers[il].attn_norm,
                 model.layers[il].attn_norm_b,
@@ -96,7 +103,7 @@ llama_model_gpt2::graph::graph(const llama_model & model, const llm_graph_params
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -129,7 +136,15 @@ llama_model_gpt2::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = build_norm(inpL,
             model.output_norm,
diff --git a/src/models/grok.cpp b/src/models/grok.cpp
index cb6afc3a70b504e8be9657400ca7bc6947594084..f3542291d3e75aa4f4a5c0d3f0ff56d74bbb2704 100644
--- a/src/models/grok.cpp
+++ b/src/models/grok.cpp
@@ -92,16 +92,23 @@ llama_model_grok::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -136,7 +143,7 @@ llama_model_grok::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f, il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -195,7 +202,15 @@ llama_model_grok::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/hunyuan-moe.cpp b/src/models/hunyuan-moe.cpp
index cedc3b53eba429f775dbf25e3c95692e7e64faa9..352caa0465b8e7c67af58987f73b43b338204609 100644
--- a/src/models/hunyuan-moe.cpp
+++ b/src/models/hunyuan-moe.cpp
@@ -62,7 +62,12 @@ llama_model_hunyuan_moe::graph::graph(const llama_model & model, const llm_graph
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -71,9 +76,11 @@ llama_model_hunyuan_moe::graph::graph(const llama_model & model, const llm_graph
 
     const float kq_scale = 1.0f / sqrtf(float(n_embd_head));
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -122,7 +129,7 @@ llama_model_hunyuan_moe::graph::graph(const llama_model & model, const llm_graph
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
             cb(cur, "attn_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -168,7 +175,15 @@ llama_model_hunyuan_moe::graph::graph(const llama_model & model, const llm_graph
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/hy-v4.cpp b/src/models/hy-v4.cpp
index ee41787ba3e59d86dd7e660d63b1fc60e12a50fb..b8ec8ed772ef65631aee64252e2f0efa2262e8de 100644
--- a/src/models/hy-v4.cpp
+++ b/src/models/hy-v4.cpp
@@ -504,21 +504,37 @@ llama_model_hy_v4::graph::graph(const llama_model & model, const llm_graph_param
 
     const bool is_dsa = hparams.indexer_top_k > 0;
 
-    ggml_tensor * inp = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * inp = (!stage_filtered || il_start == 0) ? build_inp_embd(model.tok_embd) : nullptr;
     ggml_tensor * inp_pos = build_inp_pos();
     llm_graph_input_attn_k     * inp_attn     = is_dsa ? nullptr : build_attn_inp_k();
     llm_graph_input_attn_k_dsa * inp_attn_dsa = is_dsa ? build_attn_inp_k_dsa() : nullptr;
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     // top-k of the last "full" indexer layer, reused by the following "shared" layers
     ggml_tensor * last_top_k = nullptr;
 
     // expand the single embedding into hc parallel residual streams
-    ggml_tensor * inpL = ggml_reshape_3d(ctx0, inp, n_embd, 1, n_tokens);
-    inpL = ggml_repeat_4d(ctx0, inpL, n_embd, hc, n_tokens, 1);
+    ggml_tensor * inpL = stage_filtered && il_start > 0 ? nullptr : ggml_reshape_3d(ctx0, inp, n_embd, 1, n_tokens);
+    if (stage_filtered && il_start > 0) {
+        auto stage_inp = std::make_unique<llm_graph_input_hyperconnection>(n_embd, hc);
+        stage_inp->values = ggml_new_tensor_3d(ctx0, GGML_TYPE_F32, n_embd, hc, n_tokens);
+        cb(stage_inp->values, "hc_stage_input", -1);
+        ggml_set_input(stage_inp->values);
+        inpL = stage_inp->values;
+        res->t_skippy_activation_input = inpL;
+        res->add_input(std::move(stage_inp));
+    }
+    inpL = stage_filtered && il_start > 0 ? inpL : ggml_repeat_4d(ctx0, inpL, n_embd, hc, n_tokens, 1);
     cb(inpL, "hc_init", -1);
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * residual = inpL;
         ggml_tensor * post = nullptr;
 
@@ -577,7 +593,16 @@ llama_model_hy_v4::graph::graph(const llama_model & model, const llm_graph_param
 
         inpL = build_hc_post(cur, residual, post, il);
         cb(inpL, "l_out", il);
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_skippy_activation_output = inpL;
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     // prune to the requested output rows once, after all HC streams are done
     if (inp_out_ids) {
diff --git a/src/models/inkling.cpp b/src/models/inkling.cpp
index e7a574e5fc1c71f954096e35a06395c61cee0947..a1f3678621b707ffa018bc37ce04baf1a48eb0b5 100644
--- a/src/models/inkling.cpp
+++ b/src/models/inkling.cpp
@@ -622,20 +622,35 @@ llama_model_inkling::graph::graph(const llama_model & model, const llm_graph_par
         return;
     }
 
-    ggml_tensor * cur = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * cur = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     // mtmd embd rows arrive pre-normalized; embed_norm applies to text token lookups only
-    if (ubatch.token) {
+    if ((!stage_filtered || il_start == 0) && (ubatch.token)) {
         cur = build_norm(cur, model.tok_norm, NULL, LLM_NORM_RMS, -1);
     }
     cb(cur, ubatch.token ? "inkling_embd_norm" : "inkling_mm_embd", -1);
     ggml_build_forward_expand(gf, cur);
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(cur, il);
+
         res->t_layer_inp[il] = cur;
         cur = build_block(cur, il);
+        end_block(cur, il);
+    }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(cur, "stage_boundary", il_end - 1);
+        res->t_embd = cur;
+        ggml_build_forward_expand(gf, cur);
+        return;
     }
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     // Conv states need every layer to see all tokens. Intermediate stages hand off the full
     // activation sequence, and the final stage trims outputs only after its last local layer.
diff --git a/src/models/llada-moe.cpp b/src/models/llada-moe.cpp
index 0ee9ce1bec5fde2e542cb69fcb9d8e9e85155906..563550b9f43e12e1697089f99ccdb1095fafa574 100644
--- a/src/models/llada-moe.cpp
+++ b/src/models/llada-moe.cpp
@@ -60,16 +60,23 @@ llama_model_llada_moe::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -110,7 +117,7 @@ llama_model_llada_moe::graph::graph(const llama_model & model, const llm_graph_p
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -143,7 +150,15 @@ llama_model_llada_moe::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/llada.cpp b/src/models/llada.cpp
index ae3d6925c136177c39640fca0e7399e7af426f5f..f8356a2d11d3b4e6fa3cb7328f218b68a91781ba 100644
--- a/src/models/llada.cpp
+++ b/src/models/llada.cpp
@@ -73,7 +73,12 @@ llama_model_llada::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -81,9 +86,11 @@ llama_model_llada::graph::graph(const llama_model & model, const llm_graph_param
     // Non-causal attention for diffusion
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -110,7 +117,7 @@ llama_model_llada::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -135,7 +142,15 @@ llama_model_llada::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/llama4.cpp b/src/models/llama4.cpp
index 8a812beffac4b3b7702c1ccb71b3ae426d34c76e..a07452e9d29b7e4703699195196815325840885d 100644
--- a/src/models/llama4.cpp
+++ b/src/models/llama4.cpp
@@ -113,14 +113,29 @@ llama_model_llama4::graph<iswa>::graph(const llama_model & model, const llm_grap
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     // temperature tuning
     ggml_tensor * inp_attn_scale = nullptr;
-    inp_attn_scale = build_inp_attn_scale();
+    bool skippy_uses_attention_scale = false;
+    for (int skippy_il = il_start; skippy_il < il_end; ++skippy_il) {
+        const bool skippy_use_rope = hparams.n_no_rope_layer_step > 0 &&
+                (skippy_il + 1) % hparams.n_no_rope_layer_step != 0;
+        if (!skippy_use_rope) {
+            skippy_uses_attention_scale = true;
+            break;
+        }
+    }
+
+    inp_attn_scale = skippy_uses_attention_scale ? build_inp_attn_scale() : nullptr;
 
     using inp_attn_type = std::conditional_t<iswa, llm_graph_input_attn_kv_iswa, llm_graph_input_attn_kv>;
     inp_attn_type * inp_attn = nullptr;
@@ -133,9 +148,11 @@ llama_model_llama4::graph<iswa>::graph(const llama_model & model, const llm_grap
 
     const float kq_scale = hparams.f_attention_scale == 0.0f ? 1.0f/sqrtf(float(n_embd_head)) : hparams.f_attention_scale;
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         const float freq_base_l  = model.get_rope_freq_base (cparams, il);
         const float freq_scale_l = model.get_rope_freq_scale(cparams, il);
 
@@ -191,7 +208,7 @@ llama_model_llama4::graph<iswa>::graph(const llama_model & model, const llm_grap
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
             cb(cur, "attn_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -250,7 +267,15 @@ llama_model_llama4::graph<iswa>::graph(const llama_model & model, const llm_grap
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
+    }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
     }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/mimo2.cpp b/src/models/mimo2.cpp
index 8772319f4611abde0bf51a55ada4b633decff7b9..d8a659d1b7c0c2f6fb120414ed4afb078acd3de3 100644
--- a/src/models/mimo2.cpp
+++ b/src/models/mimo2.cpp
@@ -92,17 +92,24 @@ llama_model_mimo2::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
     auto * inp_attn = build_attn_inp_kv_iswa();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     const float v_scale = hparams.f_attn_value_scale;
     const bool emit_h_nextn = cparams.embeddings_nextn;
     const bool crop_last_layer = inp_out_ids && (!emit_h_nextn || cparams.embeddings_nextn_masked);
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * inpSA = inpL;
 
         uint32_t n_head_l    = hparams.n_head(il);
@@ -226,7 +233,15 @@ llama_model_mimo2::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/minimax-01.cpp b/src/models/minimax-01.cpp
index 361114acc327296f8b8a16b7d9ba741fe732ec32..192e13544c5ca1bcb90ec26a3f2b32575099ccf4 100644
--- a/src/models/minimax-01.cpp
+++ b/src/models/minimax-01.cpp
@@ -205,13 +205,26 @@ llama_model_minimax_01::graph::graph(const llama_model & model, const llm_graph_
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     auto * inp_hybrid = build_inp_mem_hybrid();
     auto * inp_rs = inp_hybrid->get_recr();
 
-    ggml_tensor * inp_pos = build_inp_pos();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    bool skippy_has_attention_layer = false;
+    for (int skippy_il = il_start; skippy_il < il_end; ++skippy_il) {
+        if (!hparams.is_recr(skippy_il)) {
+            skippy_has_attention_layer = true;
+            break;
+        }
+    }
+
+    ggml_tensor * inp_pos = skippy_has_attention_layer ? build_inp_pos() : nullptr;
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     llm_graph_input_la * la = nullptr;
 
@@ -237,7 +250,9 @@ llama_model_minimax_01::graph::graph(const llama_model & model, const llm_graph_
 
     ggml_tensor * slopes = la->inp_slopes;
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         res->t_layer_inp[il] = inpL;
 
         ggml_tensor * inpSA = inpL;
@@ -418,7 +433,7 @@ llama_model_minimax_01::graph::graph(const llama_model & model, const llm_graph_
             cb(cur, "attn_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
             residual = ggml_get_rows(ctx0, residual, inp_out_ids);
@@ -462,8 +477,16 @@ llama_model_minimax_01::graph::graph(const llama_model & model, const llm_graph_
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
+    }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
     }
 
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/minimax-m2.cpp b/src/models/minimax-m2.cpp
index 7a22af036bc74e6609719fd2c04e2c6395241b8e..6fa93481f6f553db996d26ab5a64612d0064c95a 100644
--- a/src/models/minimax-m2.cpp
+++ b/src/models/minimax-m2.cpp
@@ -53,13 +53,20 @@ llama_model_minimax_m2::graph::graph(const llama_model & model, const llm_graph_
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
     auto inp_attn = build_attn_inp_kv();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         res->t_layer_inp[il] = inpL;
 
         ggml_tensor * inpSA = inpL;
@@ -113,7 +120,7 @@ llama_model_minimax_m2::graph::graph(const llama_model & model, const llm_graph_
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -147,7 +154,15 @@ llama_model_minimax_m2::graph::graph(const llama_model & model, const llm_graph_
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/minimax-m3.cpp b/src/models/minimax-m3.cpp
index 89c7e2810788c321acf29836042ecd52200962d6..de413aec4a6888ac0e52d6b61f1825ce4482d4e6 100644
--- a/src/models/minimax-m3.cpp
+++ b/src/models/minimax-m3.cpp
@@ -212,7 +212,12 @@ llama_model_minimax_m3::graph::graph(const llama_model & model, const llm_graph_
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
 
@@ -298,9 +303,11 @@ llama_model_minimax_m3::graph::graph(const llama_model & model, const llm_graph_
         msa = (llm_graph_input_msa *) res->add_input(std::move(inp));
     }
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // self-attention
@@ -536,7 +543,7 @@ llama_model_minimax_m3::graph::graph(const llama_model & model, const llm_graph_
             }
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -591,7 +598,15 @@ llama_model_minimax_m3::graph::graph(const llama_model & model, const llm_graph_
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/modern-bert.cpp b/src/models/modern-bert.cpp
index f3e9407e0125cca79bd040d3e88ecb4c58b43168..29759c6c38e836e519bc46959c809fc3dfa13317 100644
--- a/src/models/modern-bert.cpp
+++ b/src/models/modern-bert.cpp
@@ -81,18 +81,25 @@ llama_model_modern_bert::graph::graph(const llama_model & model, const llm_graph
     ggml_tensor * inp_pos = build_inp_pos();
 
     // construct input embeddings (token, type, position)
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     cb(inpL, "inp_embd", -1);
 
     // embed layer norm
-    inpL = build_norm(inpL, model.tok_norm, nullptr, LLM_NORM, 0);
+    inpL = stage_filtered && il_start > 0 ? inpL : build_norm(inpL, model.tok_norm, nullptr, LLM_NORM, 0);
     cb(inpL, "inp_norm", 0);
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         const float freq_base_l  = model.get_rope_freq_base(cparams, il);
         const float freq_scale_l = model.get_rope_freq_scale(cparams, il);
 
@@ -132,7 +139,7 @@ llama_model_modern_bert::graph::graph(const llama_model & model, const llm_graph
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         cb(cur, "kqv_out", il);
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -160,7 +167,15 @@ llama_model_modern_bert::graph::graph(const llama_model & model, const llm_graph
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/muse-glimmer.cpp b/src/models/muse-glimmer.cpp
index 0e94153088a954e9d5b61b31dbd70dfd959bf056..0fdbcfde140515f448697f11022d72930c1abf80 100644
--- a/src/models/muse-glimmer.cpp
+++ b/src/models/muse-glimmer.cpp
@@ -70,17 +70,24 @@ llama_model_muse_glimmer::graph::graph(const llama_model & model, const llm_grap
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
-    inpL = build_norm(inpL, nullptr, nullptr, LLM_NORM_RMS, -1);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
+    inpL = stage_filtered && il_start > 0 ? inpL : build_norm(inpL, nullptr, nullptr, LLM_NORM_RMS, -1);
     cb(inpL, "embd_norm", -1);
 
     ggml_tensor * inp_pos = build_inp_pos();
     auto * inp_attn = build_attn_inp_kv_iswa();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     const float kq_scale = 1.0f / sqrtf(float(n_embd_head));
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         // expose per-layer residual for speculative drafts (see LLM_KV_TARGET_LAYERS).
         res->t_layer_inp[il] = inpL;
 
@@ -147,7 +154,7 @@ llama_model_muse_glimmer::graph::graph(const llama_model & model, const llm_grap
         cur = ggml_mul(ctx0, cur, model.layers[il].attn_post_norm);
         cb(cur, "attn_post_norm", il);
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -177,7 +184,15 @@ llama_model_muse_glimmer::graph::graph(const llama_model & model, const llm_grap
         cb(cur, "l_out", il);
 
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/nanbeige.cpp b/src/models/nanbeige.cpp
index 7d5a6bbdb6b413402ce911f8c442564834b1a731..57c8b3f8608155ed7321e0b1fed92653c1b47413 100644
--- a/src/models/nanbeige.cpp
+++ b/src/models/nanbeige.cpp
@@ -90,7 +90,12 @@ llama_model_nanbeige::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
 
@@ -100,9 +105,11 @@ llama_model_nanbeige::graph::graph(const llama_model & model, const llm_graph_pa
         ? 1.0f / sqrtf(float(n_embd_head))
         : hparams.f_attention_scale;
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         res->t_layer_inp[il] = inpL;
         ggml_tensor * inpSA = inpL;
 
@@ -135,7 +142,7 @@ llama_model_nanbeige::graph::graph(const llama_model & model, const llm_graph_pa
             cb(cur, "attn_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0, cur,   inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -169,7 +176,15 @@ llama_model_nanbeige::graph::graph(const llama_model & model, const llm_graph_pa
             cb(cur, "loop_norm", il);
             inpL = cur;
         }
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/neo-bert.cpp b/src/models/neo-bert.cpp
index 4a08d7abd40f1ba2fd658c6fe4e048dda88f30a3..56499be1b839cd060d3fcdaac6a881341904012d 100644
--- a/src/models/neo-bert.cpp
+++ b/src/models/neo-bert.cpp
@@ -50,14 +50,21 @@ llama_model_neo_bert::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * inp_pos = build_inp_pos();
 
     // construct input embeddings (token, type, position)
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     cb(inpL, "inp_embd", -1);
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * cur = inpL;
 
         // pre-norm
@@ -91,7 +98,7 @@ llama_model_neo_bert::graph::graph(const llama_model & model, const llm_graph_pa
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
             cb(cur, "kqv_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -120,7 +127,15 @@ llama_model_neo_bert::graph::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/olmo.cpp b/src/models/olmo.cpp
index 9f7a2ba60efb8af5f4e521e22ed42a1be054a19e..88cfbccd436bc39dd691bdc861d379eef8aaacec 100644
--- a/src/models/olmo.cpp
+++ b/src/models/olmo.cpp
@@ -49,16 +49,23 @@ llama_model_olmo::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -93,7 +100,7 @@ llama_model_olmo::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, nullptr, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -122,7 +129,15 @@ llama_model_olmo::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/olmoe.cpp b/src/models/olmoe.cpp
index 11c53f3f4c9c785a50dc85d16f8893c06b61eb53..9ccb787abca157ee570c567c00f9c2da96577afb 100644
--- a/src/models/olmoe.cpp
+++ b/src/models/olmoe.cpp
@@ -59,16 +59,23 @@ llama_model_olmoe::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -120,7 +127,7 @@ llama_model_olmoe::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -153,7 +160,15 @@ llama_model_olmoe::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/openelm.cpp b/src/models/openelm.cpp
index 13120bd3236d9b35a780235ba8a1fa19674b2c9b..5a82ac6caa8660f48d7f0c904101fbdfad3ce50b 100644
--- a/src/models/openelm.cpp
+++ b/src/models/openelm.cpp
@@ -54,16 +54,23 @@ llama_model_openelm::graph::graph(const llama_model & model, const llm_graph_par
 
     ggml_tensor * cur;
     ggml_tensor * inpL;
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         const int64_t n_head    = hparams.n_head(il);
         const int64_t n_head_kv = hparams.n_head_kv(il);
         const int64_t n_head_qkv = 2*n_head_kv + n_head;
@@ -123,7 +130,7 @@ llama_model_openelm::graph::graph(const llama_model & model, const llm_graph_par
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             residual = ggml_get_rows(ctx0, residual, inp_out_ids);
             cur      = ggml_get_rows(ctx0, cur,      inp_out_ids);
         }
@@ -151,7 +158,15 @@ llama_model_openelm::graph::graph(const llama_model & model, const llm_graph_par
         cb(cur, "l_out", il);
 
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     // norm
diff --git a/src/models/orion.cpp b/src/models/orion.cpp
index 863a2822269b62e67a6a3ab700ce2ffa4267a6f8..e7f609686765ff8ead89cb00e4c1d980e649c139 100644
--- a/src/models/orion.cpp
+++ b/src/models/orion.cpp
@@ -49,16 +49,23 @@ llama_model_orion::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -93,7 +100,7 @@ llama_model_orion::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -121,7 +128,15 @@ llama_model_orion::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/paddleocr.cpp b/src/models/paddleocr.cpp
index d39220bd7781179b74be848993b3bc5a43556c71..321fdf9d466e959b0d37f22a97ba5e1bbc7faab9 100644
--- a/src/models/paddleocr.cpp
+++ b/src/models/paddleocr.cpp
@@ -17,7 +17,12 @@ llama_model_paddleocr::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     int sections[4];
     std::copy(std::begin(hparams.rope_sections), std::begin(hparams.rope_sections) + 4, sections);
@@ -27,9 +32,11 @@ llama_model_paddleocr::graph::graph(const llama_model & model, const llm_graph_p
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -89,7 +96,15 @@ llama_model_paddleocr::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/pangu-embed.cpp b/src/models/pangu-embed.cpp
index 90f05c088c10755d9eafeb489efe28a95900d311..f35ef305abeadabb71da064f94f00ea5f97d1691 100644
--- a/src/models/pangu-embed.cpp
+++ b/src/models/pangu-embed.cpp
@@ -64,16 +64,23 @@ llama_model_pangu_embed::graph::graph(const llama_model & model, const llm_graph
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -108,7 +115,7 @@ llama_model_pangu_embed::graph::graph(const llama_model & model, const llm_graph
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -137,7 +144,15 @@ llama_model_pangu_embed::graph::graph(const llama_model & model, const llm_graph
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/plamo.cpp b/src/models/plamo.cpp
index 246144519e43fff01c5d569be6083848cad72c28..0cc333a9d210df5e9f89aaf230259804df995b74 100644
--- a/src/models/plamo.cpp
+++ b/src/models/plamo.cpp
@@ -45,16 +45,23 @@ llama_model_plamo::graph::graph(const llama_model & model, const llm_graph_param
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         // norm
         cur = build_norm(inpL,
                 model.layers[il].attn_norm, NULL,
@@ -89,7 +96,7 @@ llama_model_plamo::graph::graph(const llama_model & model, const llm_graph_param
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur    = ggml_get_rows(ctx0,    cur, inp_out_ids);
             sa_inp = ggml_get_rows(ctx0, sa_inp, inp_out_ids);
             inpL   = ggml_get_rows(ctx0,   inpL, inp_out_ids);
@@ -116,7 +123,15 @@ llama_model_plamo::graph::graph(const llama_model & model, const llm_graph_param
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/plamo2.cpp b/src/models/plamo2.cpp
index d946b3cff6da23f4d48c99b2fd1fd0bdf77803cb..460a3bc4ac3dbd2111b84dd3b268dd0949b0fb05 100644
--- a/src/models/plamo2.cpp
+++ b/src/models/plamo2.cpp
@@ -113,16 +113,31 @@ llama_model_plamo2::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * inpL;
 
     // {n_embd, n_tokens}
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     cb(inpL, "embedding_output", -1);
 
-    ggml_tensor * inp_pos = build_inp_pos();
+    bool skippy_has_attention_layer = false;
+    for (int skippy_il = il_start; skippy_il < il_end; ++skippy_il) {
+        if (!hparams.is_recr(skippy_il)) {
+            skippy_has_attention_layer = true;
+            break;
+        }
+    }
+
+    ggml_tensor * inp_pos = skippy_has_attention_layer ? build_inp_pos() : nullptr;
 
     auto * inp_hybrid = build_inp_mem_hybrid();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * residual = inpL;
 
         // ggml_graph_add_node(gf, model.layers[il].attn_norm);
@@ -167,7 +182,7 @@ llama_model_plamo2::graph::graph(const llama_model & model, const llm_graph_para
         cur = build_norm(cur, model.layers[il].ffn_post_norm, NULL, LLM_NORM_RMS, il);
         cb(cur, "ffn_post_norm", il);
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur      = ggml_get_rows(ctx0, cur, inp_out_ids);
             residual = ggml_get_rows(ctx0, residual, inp_out_ids);
         }
@@ -178,7 +193,15 @@ llama_model_plamo2::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/plamo3.cpp b/src/models/plamo3.cpp
index 16d0b1dcef71097371474bb1a204a81d3ec9bf2c..fe92d680a8d7c39beecd9473359aa6073b7df145 100644
--- a/src/models/plamo3.cpp
+++ b/src/models/plamo3.cpp
@@ -74,7 +74,12 @@ llama_model_plamo3::graph<iswa>::graph(const llama_model & model, const llm_grap
     const int64_t head_dim_v = hparams.n_embd_head_v();
 
     ggml_tensor * cur;
-    ggml_tensor * inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     ggml_tensor * inp_pos = build_inp_pos();
 
     using inp_attn_type = std::conditional_t<iswa, llm_graph_input_attn_kv_iswa, llm_graph_input_attn_kv>;
@@ -86,9 +91,11 @@ llama_model_plamo3::graph<iswa>::graph(const llama_model & model, const llm_grap
         inp_attn = build_attn_inp_kv();
     }
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * residual = inpL;
 
         float freq_base_l  = 0.0f;
@@ -144,7 +151,7 @@ llama_model_plamo3::graph<iswa>::graph(const llama_model & model, const llm_grap
                 Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, attn_scale, il);
         cb(cur, "attn_out", il);
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur      = ggml_get_rows(ctx0, cur, inp_out_ids);
             residual = ggml_get_rows(ctx0, residual, inp_out_ids);
         }
@@ -179,7 +186,15 @@ llama_model_plamo3::graph<iswa>::graph(const llama_model & model, const llm_grap
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/plm.cpp b/src/models/plm.cpp
index 5abefd53ba8fd5b010cab1d740701faedef25e06..7c16604f3abe17bc0e506694b6af18a4d5870329 100644
--- a/src/models/plm.cpp
+++ b/src/models/plm.cpp
@@ -57,16 +57,23 @@ llama_model_plm::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * inpL;
 
     // {n_embd, n_tokens}
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -160,7 +167,7 @@ llama_model_plm::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     q_states, k_states, v_states, nullptr, nullptr, nullptr, kq_scale, il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -187,7 +194,15 @@ llama_model_plm::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/pockettts.cpp b/src/models/pockettts.cpp
index 1b3bb6c648af6ad96be46f8dfdc2d92ab3529f2c..1acfca90f69a087091aa1cf1192f016ce580fd51 100644
--- a/src/models/pockettts.cpp
+++ b/src/models/pockettts.cpp
@@ -54,15 +54,22 @@ llama_model_pockettts::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         cur = build_norm(inpL,
                 model.layers[il].attn_norm,
                 model.layers[il].attn_norm_b,
@@ -95,7 +102,7 @@ llama_model_pockettts::graph::graph(const llama_model & model, const llm_graph_p
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -127,7 +134,15 @@ llama_model_pockettts::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = build_norm(inpL,
             model.output_norm,
diff --git a/src/models/qwen.cpp b/src/models/qwen.cpp
index 1f5dff3843cf385b487a5de76da867e5d1e9a3eb..886e96356bccd74c61693d920d2ccf7f4cad8bcc 100644
--- a/src/models/qwen.cpp
+++ b/src/models/qwen.cpp
@@ -48,16 +48,23 @@ llama_model_qwen::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         cur = build_norm(inpL,
@@ -91,7 +98,7 @@ llama_model_qwen::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -120,7 +127,15 @@ llama_model_qwen::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/refact.cpp b/src/models/refact.cpp
index a46c358fa68ae45a2e6c9c023422b74abde4be67..557fb98b52dca3b0cd5c784ee6588326980098ae 100644
--- a/src/models/refact.cpp
+++ b/src/models/refact.cpp
@@ -84,13 +84,20 @@ llama_model_refact::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         cur = build_norm(inpL,
@@ -111,7 +118,7 @@ llama_model_refact::graph::graph(const llama_model & model, const llm_graph_para
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -140,7 +147,15 @@ llama_model_refact::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/rnd1.cpp b/src/models/rnd1.cpp
index 553a757302994ab1c7bb59856b6ffac2ea422347..7a8a345d56ec387227045d46d4b34e5547cad789 100644
--- a/src/models/rnd1.cpp
+++ b/src/models/rnd1.cpp
@@ -71,7 +71,12 @@ llama_model_rnd1::graph::graph(const llama_model & model, const llm_graph_params
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -79,9 +84,11 @@ llama_model_rnd1::graph::graph(const llama_model & model, const llm_graph_params
     // Non-causal attention for diffusion
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -122,7 +129,7 @@ llama_model_rnd1::graph::graph(const llama_model & model, const llm_graph_params
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -157,7 +164,15 @@ llama_model_rnd1::graph::graph(const llama_model & model, const llm_graph_params
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/seed-oss.cpp b/src/models/seed-oss.cpp
index 57de881a091f251702879f01342a34e82305822b..734cf9bb92f80102a869a6e24ef3025234220e3b 100644
--- a/src/models/seed-oss.cpp
+++ b/src/models/seed-oss.cpp
@@ -55,7 +55,12 @@ llama_model_seed_oss::graph::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -64,9 +69,11 @@ llama_model_seed_oss::graph::graph(const llama_model & model, const llm_graph_pa
 
     const float kq_scale = hparams.f_attention_scale == 0.0f ? 1.0f/sqrtf(float(n_embd_head)) : hparams.f_attention_scale;
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -102,7 +109,7 @@ llama_model_seed_oss::graph::graph(const llama_model & model, const llm_graph_pa
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, kq_scale, il);
             cb(cur, "attn_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -131,7 +138,15 @@ llama_model_seed_oss::graph::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur,
diff --git a/src/models/smallthinker.cpp b/src/models/smallthinker.cpp
index 680ffb8fda37680385bb7dcc9bcc9607adc4b800..a5bc20ebba4860702d7d3ffab78e55921bd76260 100644
--- a/src/models/smallthinker.cpp
+++ b/src/models/smallthinker.cpp
@@ -83,7 +83,12 @@ llama_model_smallthinker::graph<iswa>::graph(const llama_model & model, const ll
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -96,9 +101,11 @@ llama_model_smallthinker::graph<iswa>::graph(const llama_model & model, const ll
     } else {
         inp_attn = build_attn_inp_kv();
     }
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         const float freq_base_l  = model.get_rope_freq_base (cparams, il);
         const float freq_scale_l = model.get_rope_freq_scale(cparams, il);
 
@@ -135,7 +142,7 @@ llama_model_smallthinker::graph<iswa>::graph(const llama_model & model, const ll
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f / sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
             probs = ggml_get_rows(ctx0, probs, inp_out_ids);
@@ -170,7 +177,15 @@ llama_model_smallthinker::graph<iswa>::graph(const llama_model & model, const ll
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
diff --git a/src/models/spark2-5.cpp b/src/models/spark2-5.cpp
index 107448777c5cf2d973f5be56d435644168cb7a80..d894970e5198855f3a6ceb57580beba31bbffb25 100644
--- a/src/models/spark2-5.cpp
+++ b/src/models/spark2-5.cpp
@@ -59,14 +59,21 @@ llama_model_spark2_5::graph::graph(const llama_model & model, const llm_graph_pa
     GGML_ASSERT(n_embd_head == hparams.n_embd_head_k());
     GGML_ASSERT(hparams.swa_type == LLAMA_SWA_TYPE_STANDARD);
 
-    ggml_tensor * inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    ggml_tensor * inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     ggml_tensor * inp_pos = build_inp_pos();
     auto * inp_attn = build_attn_inp_kv_iswa();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     const float kq_scale = 1.0f / sqrtf(float(n_embd_head));
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * inpSA = inpL;
         ggml_tensor * cur = build_norm(inpL, model.layers[il].attn_norm, nullptr, LLM_NORM_RMS, il);
         cb(cur, "attn_norm", il);
@@ -108,7 +115,7 @@ llama_model_spark2_5::graph::graph(const llama_model & model, const llm_graph_pa
         cur = build_lora_mm(model.layers[il].wo, cur, model.layers[il].wo_s);
         cb(cur, "attn_out_proj", il);
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -132,7 +139,15 @@ llama_model_spark2_5::graph::graph(const llama_model & model, const llm_graph_pa
         cb(cur, "l_out", il);
 
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     ggml_tensor * cur = build_norm(inpL, model.output_norm, nullptr, LLM_NORM_RMS, -1);
     cb(cur, "result_norm", -1);
diff --git a/src/models/starcoder.cpp b/src/models/starcoder.cpp
index f73a88fd4e9041fa1eb0aa328cd0ddebda1aabac..6596b6f7284e319a2a4e74f1b1d85ce49d95e1e1 100644
--- a/src/models/starcoder.cpp
+++ b/src/models/starcoder.cpp
@@ -65,7 +65,12 @@ llama_model_starcoder::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
@@ -75,12 +80,14 @@ llama_model_starcoder::graph::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * pos = ggml_get_rows(ctx0, model.pos_embd, inp_pos);
     cb(pos, "pos_embd", -1);
 
-    inpL = ggml_add(ctx0, inpL, pos);
+    inpL = stage_filtered && il_start > 0 ? inpL : ggml_add(ctx0, inpL, pos);
     cb(inpL, "inpL", -1);
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         cur = build_norm(inpL,
                 model.layers[il].attn_norm,
                 model.layers[il].attn_norm_b,
@@ -96,7 +103,7 @@ llama_model_starcoder::graph::graph(const llama_model & model, const llm_graph_p
                     model.layers[il].wo, model.layers[il].wo_b, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur  = ggml_get_rows(ctx0,  cur, inp_out_ids);
             inpL = ggml_get_rows(ctx0, inpL, inp_out_ids);
         }
@@ -127,7 +134,15 @@ llama_model_starcoder::graph::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = build_norm(inpL,
             model.output_norm,
             model.output_norm_b,
diff --git a/src/models/step35.cpp b/src/models/step35.cpp
index 946a3696000f1175d7b2780e15308d349cb3d339..ff812bf693a106e378b426f965cd7400a22dc3b5 100644
--- a/src/models/step35.cpp
+++ b/src/models/step35.cpp
@@ -192,13 +192,20 @@ llama_model_step35::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
     ggml_tensor * inp_pos     = build_inp_pos();
     auto        * inp_attn    = build_attn_inp_kv_iswa();
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     // MTP/NextN layers are loaded as extra decoder blocks but not executed in the main pass.
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * inpSA = inpL;
 
         const uint32_t n_head_l    = hparams.n_head(il);
@@ -287,7 +294,7 @@ llama_model_step35::graph::graph(const llama_model & model, const llm_graph_para
             cb(cur, "attn_proj", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids && cparams.embeddings_nextn_masked) {
+        if (il == il_end - 1 && inp_out_ids && cparams.embeddings_nextn_masked) {
             cur   = ggml_get_rows(ctx0, cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -342,7 +349,15 @@ llama_model_step35::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/t5.cpp b/src/models/t5.cpp
index b0e3f062572f63ad5e5a92941ef15b5894c96fcd..768bdc4dbb53767d846be96ed169228a9a7ca00a 100644
--- a/src/models/t5.cpp
+++ b/src/models/t5.cpp
@@ -128,7 +128,12 @@ llama_model_t5::graph<false>::graph(const llama_model & model, const llm_graph_p
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : hparams.dec_n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * embd_enc       = build_inp_cross_embd();
     ggml_tensor * pos_bucket_dec = build_inp_pos_bucket_dec();
@@ -138,11 +143,13 @@ llama_model_t5::graph<false>::graph(const llama_model & model, const llm_graph_p
     auto * inp_attn_self  = build_attn_inp_kv();
     auto * inp_attn_cross = build_attn_inp_cross();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     const int64_t dec_n_layer = hparams.dec_n_layer;
 
-    for (int il = 0; il < dec_n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -253,7 +260,15 @@ llama_model_t5::graph<false>::graph(const llama_model & model, const llm_graph_p
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
     cb(cur, "result_embd", -1);
 
@@ -282,15 +297,22 @@ llama_model_t5::graph<true>::graph(const llama_model & model, const llm_graph_pa
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     ggml_tensor * pos_bucket_enc = build_inp_pos_bucket_enc();
 
     auto * inp_attn = build_attn_inp_no_cache();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         // norm
@@ -322,7 +344,7 @@ llama_model_t5::graph<true>::graph(const llama_model & model, const llm_graph_pa
                     Qcur, Kcur, Vcur, kq_b, nullptr, nullptr, 1.0f, il);
             cb(cur, "kqv_out", il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -355,7 +377,15 @@ llama_model_t5::graph<true>::graph(const llama_model & model, const llm_graph_pa
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
     cb(cur, "result_embd", -1);
 
diff --git a/src/models/talkie.cpp b/src/models/talkie.cpp
index 393e8f65bf4d54618ec19afc4e430873ea1bbb9b..fe44269610f3908da9e6bd81d641fa562da036c3 100644
--- a/src/models/talkie.cpp
+++ b/src/models/talkie.cpp
@@ -46,8 +46,13 @@ llama_model_talkie::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
-    inpL = build_norm(inpL, nullptr, nullptr, LLM_NORM_RMS, -1);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
+    inpL = stage_filtered && il_start > 0 ? inpL : build_norm(inpL, nullptr, nullptr, LLM_NORM_RMS, -1);
     cb(inpL, "inp_norm", -1);
 
     ggml_tensor * embd_skip = inpL;
@@ -57,11 +62,13 @@ llama_model_talkie::graph::graph(const llama_model & model, const llm_graph_para
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
 
     const float kq_scale = 1.0f / sqrtf(float(n_embd_head));
 
-    for (int il = 0; il < n_layer; ++il) {
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
+
         ggml_tensor * inpSA = inpL;
         ggml_tensor * inp_skip = embd_skip;
 
@@ -98,7 +105,7 @@ llama_model_talkie::graph::graph(const llama_model & model, const llm_graph_para
             cb(cur, "attn_out", il);
         }
 
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur      = ggml_get_rows(ctx0, cur,      inp_out_ids);
             inpSA    = ggml_get_rows(ctx0, inpSA,    inp_out_ids);
             inp_skip = ggml_get_rows(ctx0, inp_skip, inp_out_ids);
@@ -130,7 +137,15 @@ llama_model_talkie::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
 
     cur = inpL;
 
diff --git a/src/models/xverse.cpp b/src/models/xverse.cpp
index 3135001293a8de7da4d7c5c275fd4cdbd0b6e1c9..2539cfdf1048d0b80412de666ef23e1ce0db7c19 100644
--- a/src/models/xverse.cpp
+++ b/src/models/xverse.cpp
@@ -47,16 +47,23 @@ llama_model_xverse::graph::graph(const llama_model & model, const llm_graph_para
     ggml_tensor * cur;
     ggml_tensor * inpL;
 
-    inpL = build_inp_embd(model.tok_embd);
+    const skippy_graph_filter & stage_filter = build_inputs.filter;
+    const bool stage_filtered = stage_filter.enabled;
+    const int il_start = stage_filtered ? stage_filter.layer_start : 0;
+    const int il_end   = stage_filtered ? stage_filter.layer_end   : n_layer;
+
+    inpL = build_inp_embd(stage_filtered && il_start > 0 ? nullptr : model.tok_embd);
 
     // inp_pos - contains the positions
     ggml_tensor * inp_pos = build_inp_pos();
 
     auto * inp_attn = build_attn_inp_kv();
 
-    ggml_tensor * inp_out_ids = build_inp_out_ids();
+    ggml_tensor * inp_out_ids = (!stage_filtered || stage_filter.include_output) ? build_inp_out_ids() : nullptr;
+
+    for (int il = il_start; il < il_end; ++il) {
+        begin_block(inpL, il);
 
-    for (int il = 0; il < n_layer; ++il) {
         ggml_tensor * inpSA = inpL;
 
         cur = build_norm(inpL,
@@ -89,7 +96,7 @@ llama_model_xverse::graph::graph(const llama_model & model, const llm_graph_para
                     model.layers[il].wo, NULL, model.layers[il].wo_s,
                     Qcur, Kcur, Vcur, nullptr, nullptr, nullptr, 1.0f/sqrtf(float(n_embd_head)), il);
         }
-        if (il == n_layer - 1 && inp_out_ids) {
+        if (il == il_end - 1 && inp_out_ids) {
             cur   = ggml_get_rows(ctx0,   cur, inp_out_ids);
             inpSA = ggml_get_rows(ctx0, inpSA, inp_out_ids);
         }
@@ -118,7 +125,15 @@ llama_model_xverse::graph::graph(const llama_model & model, const llm_graph_para
 
         // input for next layer
         inpL = cur;
+        end_block(inpL, il);
     }
+    if (stage_filtered && !stage_filter.include_output) {
+        cb(inpL, "stage_boundary", il_end - 1);
+        res->t_embd = inpL;
+        ggml_build_forward_expand(gf, inpL);
+        return;
+    }
+
     cur = inpL;
 
     cur = build_norm(cur, model.output_norm, NULL, LLM_NORM_RMS, -1);
-- 
2.54.0

