From c81c80523566dc9240dc6080c37cd33e6be0b478 Mon Sep 17 00:00:00 2001
From: scama
 <a1860575018c4680d5669dd7bc3bd356b478bccb8d42e194df46304a5e25f49a@meshllm.communities.buzz.xyz>
Date: Tue, 15 Sep 2026 14:33:49 +1000
Subject: [PATCH] skippy: enforce reasoning token budgets

---
 common/CMakeLists.txt           |   1 -
 common/reasoning-budget.cpp     | 119 ++++++++++++++++++--------------
 common/reasoning-budget.h       |  16 ++---
 common/stage-chat.cpp           |   3 +
 include/skippy/common.h         |   2 +-
 include/skippy/sampling.h       |   2 +
 src/CMakeLists.txt              |   5 +-
 src/skippy/runtime_types.h      |   2 +
 src/skippy/sampling.cpp         | 107 +++++++++++++++++++++++++++-
 src/skippy/sampling_internal.h  |   5 ++
 src/skippy/session.cpp          |   4 ++
 tests/test-reasoning-budget.cpp |   1 +
 12 files changed, 202 insertions(+), 65 deletions(-)

diff --git a/common/CMakeLists.txt b/common/CMakeLists.txt
index 732fafee2..650e14517 100644
--- a/common/CMakeLists.txt
+++ b/common/CMakeLists.txt
@@ -101,7 +101,6 @@ add_library(${TARGET}
     peg-parser.h
     preset.cpp
     preset.h
-    reasoning-budget.cpp
     reasoning-budget.h
     sampling.cpp
     sampling.h
diff --git a/common/reasoning-budget.cpp b/common/reasoning-budget.cpp
index 4884299f3..8fd446e67 100644
--- a/common/reasoning-budget.cpp
+++ b/common/reasoning-budget.cpp
@@ -1,9 +1,4 @@
 #include "reasoning-budget.h"
-#include "common.h"
-#include "trie.h"
-#include "unicode.h"
-
-#include "log.h"
 
 #include <algorithm>
 #include <cmath>
@@ -12,14 +7,18 @@
 #include <vector>
 
 struct token_matcher {
-    std::vector<llama_tokens> seqs;
-    common_aho_corasick ac;
-    size_t state = 0;
+    std::vector<std::vector<llama_token>> seqs;
+    std::vector<llama_token> recent;
+    size_t max_length = 0;
 
-    token_matcher(const std::vector<llama_tokens> & seqs) : seqs(collect(seqs)), ac(build_trie(this->seqs)) {}
+    token_matcher(const std::vector<std::vector<llama_token>> & seqs) : seqs(collect(seqs)) {
+        for (const auto & seq : this->seqs) {
+            max_length = std::max(max_length, seq.size());
+        }
+    }
 
-    static std::vector<llama_tokens> collect(const std::vector<llama_tokens> & seqs) {
-        std::vector<llama_tokens> res;
+    static std::vector<std::vector<llama_token>> collect(const std::vector<std::vector<llama_token>> & seqs) {
+        std::vector<std::vector<llama_token>> res;
         for (const auto & seq : seqs) {
             if (!seq.empty() && std::find(res.begin(), res.end(), seq) == res.end()) {
                 res.push_back(seq);
@@ -28,33 +27,66 @@ struct token_matcher {
         return res;
     }
 
-    static common_trie build_trie(const std::vector<llama_tokens> & seqs) {
-        common_trie t;
-        for (const auto & seq : seqs) {
-            t.insert(std::vector<uint32_t>(seq.begin(), seq.end()));
-        }
-        return t;
-    }
-
     // returns the index into seqs of the longest sequence ending at this token, or -1
     int32_t advance(llama_token token) {
-        state = ac.next(state, (uint32_t) token);
-        const int32_t p = ac.match_pattern(state);
-        if (p >= 0) {
-            state = 0;
+        recent.push_back(token);
+        if (recent.size() > max_length) {
+            recent.erase(recent.begin());
+        }
+        int32_t match = -1;
+        size_t match_length = 0;
+        for (size_t i = 0; i < seqs.size(); ++i) {
+            const auto & seq = seqs[i];
+            if (seq.size() >= match_length && seq.size() <= recent.size() &&
+                    std::equal(seq.rbegin(), seq.rend(), recent.rbegin())) {
+                match = static_cast<int32_t>(i);
+                match_length = seq.size();
+            }
         }
-        return p;
+        if (match >= 0) {
+            recent.clear();
+        }
+        return match;
     }
 
-    void reset() { state = 0; }
+    void reset() { recent.clear(); }
 };
 
+static std::string reasoning_budget_token_to_piece(const llama_vocab * vocab, llama_token token) {
+    std::string piece;
+    piece.resize(piece.capacity());
+    int32_t length = llama_token_to_piece(vocab, token, piece.data(), piece.size(), 0, false);
+    if (length < 0) {
+        piece.resize(static_cast<size_t>(-length));
+        length = llama_token_to_piece(vocab, token, piece.data(), piece.size(), 0, false);
+    }
+    if (length < 0) {
+        return {};
+    }
+    piece.resize(static_cast<size_t>(length));
+    return piece;
+}
+
+static bool reasoning_budget_utf8_is_complete(const std::string & value) {
+    if (value.empty()) {
+        return true;
+    }
+    for (int offset = 1; offset <= std::min(4, static_cast<int>(value.size())); ++offset) {
+        const unsigned char byte = value[value.size() - offset];
+        if ((byte & 0xc0) != 0x80) {
+            const int expected = byte >= 0xf0 ? 4 : byte >= 0xe0 ? 3 : byte >= 0xc0 ? 2 : 1;
+            return offset >= expected;
+        }
+    }
+    return false;
+}
+
 struct common_reasoning_budget_ctx {
     const llama_vocab * vocab;
 
     token_matcher start_matcher;
     token_matcher end_matcher;
-    llama_tokens forced_tokens;
+    std::vector<llama_token> forced_tokens;
 
     int32_t budget;           // maximum tokens in reasoning block
     int32_t remaining;        // tokens remaining in budget
@@ -80,12 +112,9 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
             if (ctx->start_matcher.advance(token) >= 0) {
                 ctx->state = REASONING_BUDGET_COUNTING;
                 ctx->remaining = ctx->budget;
-                COM_TRC("activated, budget=%d tokens\n", ctx->budget);
-
                 if (ctx->remaining <= 0) {
                     ctx->state = REASONING_BUDGET_FORCING;
                     ctx->force_pos = 0;
-                    COM_TRC("%s", "budget=0, forcing immediately\n");
                 }
             }
             break;
@@ -97,14 +126,13 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
             if (match >= 0) {
                 ctx->state = REASONING_BUDGET_DONE;
                 ctx->end_match = match;
-                COM_TRC("%s", "deactivated (natural end)\n");
                 break;
             }
 
             bool utf8_complete = true;
             if (ctx->vocab != nullptr) {
-                const std::string piece = common_token_to_piece(ctx->vocab, token, false);
-                utf8_complete = common_utf8_is_complete(piece);
+                const std::string piece = reasoning_budget_token_to_piece(ctx->vocab, token);
+                utf8_complete = reasoning_budget_utf8_is_complete(piece);
             }
 
             if (ctx->state == REASONING_BUDGET_WAITING_UTF8) {
@@ -112,7 +140,6 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
                     ctx->state = REASONING_BUDGET_FORCING;
                     ctx->force_pos = 0;
                     ctx->end_matcher.reset();
-                    COM_TRC("%s", "UTF-8 complete, now forcing end sequence\n");
                 }
             } else if (ctx->state == REASONING_BUDGET_COUNTING) {
                 ctx->remaining--;
@@ -121,11 +148,9 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
                         ctx->state = REASONING_BUDGET_FORCING;
                         ctx->force_pos = 0;
                         ctx->end_matcher.reset();
-                        COM_TRC("%s", "budget exhausted, forcing end sequence\n");
                     } else {
                         ctx->state = REASONING_BUDGET_WAITING_UTF8;
                         ctx->end_matcher.reset();
-                        COM_TRC("%s", "budget exhausted, waiting for UTF-8 completion\n");
                     }
                 }
             }
@@ -139,7 +164,6 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
             if (ctx->force_pos >= ctx->forced_tokens.size()) {
                 ctx->state = REASONING_BUDGET_DONE;
                 ctx->end_match = match;
-                COM_TRC("%s", "forced sequence complete, done\n");
             }
             break;
         }
@@ -151,12 +175,9 @@ static void common_reasoning_budget_accept(struct llama_sampler * smpl, llama_to
                 ctx->remaining = ctx->budget;
                 ctx->end_matcher.reset();
                 ctx->end_match = -1;
-                COM_TRC("re-activated on new start tag, budget=%d tokens\n", ctx->budget);
-
                 if (ctx->remaining <= 0) {
                     ctx->state = REASONING_BUDGET_FORCING;
                     ctx->force_pos = 0;
-                    COM_TRC("%s", "budget=0, forcing immediately\n");
                 }
             }
             break;
@@ -196,8 +217,8 @@ static void common_reasoning_budget_reset(struct llama_sampler * smpl) {
 }
 
 static struct llama_sampler * common_reasoning_budget_init_state(
-        const struct llama_vocab * vocab, const std::vector<llama_tokens> & start_seqs,
-        const std::vector<llama_tokens> & end_seqs, const llama_tokens & forced_tokens,
+        const struct llama_vocab * vocab, const std::vector<std::vector<llama_token>> & start_seqs,
+        const std::vector<std::vector<llama_token>> & end_seqs, const std::vector<llama_token> & forced_tokens,
         int32_t budget, common_reasoning_budget_state initial_state);
 
 static struct llama_sampler * common_reasoning_budget_clone(const struct llama_sampler * smpl);
@@ -232,9 +253,9 @@ static struct llama_sampler * common_reasoning_budget_clone(const struct llama_s
 
 static struct llama_sampler * common_reasoning_budget_init_state(
         const struct llama_vocab        * vocab,
-        const std::vector<llama_tokens> & start_seqs,
-        const std::vector<llama_tokens> & end_seqs,
-        const llama_tokens              & forced_tokens,
+        const std::vector<std::vector<llama_token>> & start_seqs,
+        const std::vector<std::vector<llama_token>> & end_seqs,
+        const std::vector<llama_token>              & forced_tokens,
         int32_t                           budget,
         common_reasoning_budget_state     initial_state) {
     // promote COUNTING with budget <= 0 to FORCING
@@ -260,9 +281,9 @@ static struct llama_sampler * common_reasoning_budget_init_state(
 
 struct llama_sampler * common_reasoning_budget_init(
         const struct llama_vocab        * vocab,
-        const std::vector<llama_tokens> & start_seqs,
-        const std::vector<llama_tokens> & end_seqs,
-        const llama_tokens              & forced_tokens,
+        const std::vector<std::vector<llama_token>> & start_seqs,
+        const std::vector<std::vector<llama_token>> & end_seqs,
+        const std::vector<llama_token>              & forced_tokens,
         int32_t                           budget,
         common_reasoning_budget_state     initial_state) {
     return common_reasoning_budget_init_state(vocab, start_seqs, end_seqs, forced_tokens, budget, initial_state);
@@ -275,7 +296,7 @@ common_reasoning_budget_state common_reasoning_budget_get_state(const struct lla
     return ((const common_reasoning_budget_ctx *)smpl->ctx)->state;
 }
 
-const llama_tokens * common_reasoning_budget_get_end_match(const struct llama_sampler * smpl) {
+const std::vector<llama_token> * common_reasoning_budget_get_end_match(const struct llama_sampler * smpl) {
     if (!smpl) {
         return nullptr;
     }
@@ -304,7 +325,5 @@ bool common_reasoning_budget_force(struct llama_sampler * smpl) {
     ctx->state = REASONING_BUDGET_FORCING;
     ctx->force_pos = 0;
     ctx->end_matcher.reset();
-    COM_TRC("%s", "forced into forcing state (manual transition)\n");
-
     return true;
 }
diff --git a/common/reasoning-budget.h b/common/reasoning-budget.h
index 1b89a04c4..85d5c35dc 100644
--- a/common/reasoning-budget.h
+++ b/common/reasoning-budget.h
@@ -2,8 +2,6 @@
 
 #include "llama.h"
 
-#include "common.h"
-
 #include <cstdint>
 #include <vector>
 
@@ -33,20 +31,20 @@ enum common_reasoning_budget_state {
 //   budget         - max tokens allowed in the reasoning block
 //   initial_state  - initial state
 //
-struct llama_sampler * common_reasoning_budget_init(
+LLAMA_API struct llama_sampler * common_reasoning_budget_init(
         const struct llama_vocab        * vocab,
-        const std::vector<llama_tokens> & start_seqs,
-        const std::vector<llama_tokens> & end_seqs,
-        const llama_tokens              & forced_tokens,
+        const std::vector<std::vector<llama_token>> & start_seqs,
+        const std::vector<std::vector<llama_token>> & end_seqs,
+        const std::vector<llama_token>              & forced_tokens,
         int32_t                           budget,
         common_reasoning_budget_state     initial_state = REASONING_BUDGET_IDLE);
 
-common_reasoning_budget_state common_reasoning_budget_get_state(const struct llama_sampler * smpl);
+LLAMA_API common_reasoning_budget_state common_reasoning_budget_get_state(const struct llama_sampler * smpl);
 
 // The end sequence that transitioned the sampler to DONE, or nullptr if none
 // was recorded. Cleared when a new start sequence re-arms the sampler.
-const llama_tokens * common_reasoning_budget_get_end_match(const struct llama_sampler * smpl);
+LLAMA_API const std::vector<llama_token> * common_reasoning_budget_get_end_match(const struct llama_sampler * smpl);
 
 // Manually transition the reasoning budget sampler into the FORCING state.
 // Returns true if the transition occurred.
-bool common_reasoning_budget_force(struct llama_sampler * smpl);
+LLAMA_API bool common_reasoning_budget_force(struct llama_sampler * smpl);
diff --git a/common/stage-chat.cpp b/common/stage-chat.cpp
index 180c0a884..5b3f8690d 100644
--- a/common/stage-chat.cpp
+++ b/common/stage-chat.cpp
@@ -153,6 +153,9 @@ static json skippy_common_chat_metadata_json(
         {"chat_format", static_cast<int>(params.format)},
         {"reasoning_format", common_reasoning_format_name(reasoning_format)},
         {"generation_prompt", params.generation_prompt},
+        {"supports_thinking", params.supports_thinking},
+        {"thinking_start_tag", params.thinking_start_tag},
+        {"thinking_end_tags", params.thinking_end_tags},
         {"parse_tool_calls", parse_tool_calls},
         {"chat_parser", params.parser},
         {"grammar", params.grammar},
diff --git a/include/skippy/common.h b/include/skippy/common.h
index ba62a2c8b..3ad592c16 100644
--- a/include/skippy/common.h
+++ b/include/skippy/common.h
@@ -46,7 +46,7 @@ extern "C" {
 
 #define SKIPPY_ABI_VERSION_MAJOR 0
 #define SKIPPY_ABI_VERSION_MINOR 1
-#define SKIPPY_ABI_VERSION_PATCH 62
+#define SKIPPY_ABI_VERSION_PATCH 63
 
 #if defined(_MSC_VER)
 #define SKIPPY_DEPRECATED(message) __declspec(deprecated(message))
diff --git a/include/skippy/sampling.h b/include/skippy/sampling.h
index 8b114e3e4..1b42f32c0 100644
--- a/include/skippy/sampling.h
+++ b/include/skippy/sampling.h
@@ -65,6 +65,8 @@ struct skippy_sampling_config {
     uint32_t dry_sequence_breaker_count;
     char dry_sequence_breakers[SKIPPY_MAX_DRY_SEQUENCE_BREAKERS][SKIPPY_MAX_DRY_SEQUENCE_BREAKER_BYTES];
     llama_logit_bias logit_bias[SKIPPY_MAX_LOGIT_BIAS];
+    /** -1 disables enforcement; 0 closes reasoning immediately; positive values cap reasoning tokens. */
+    int32_t reasoning_budget_tokens;
 };
 
 #ifdef __cplusplus
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index de75e976a..c30e1e030 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -66,6 +66,7 @@ add_library(llama
             skippy/executable_graph.cpp
             skippy/model_package.cpp
             skippy/package_tensor_binding.cpp
+            ../common/reasoning-budget.cpp
             skippy/sampling.cpp
             skippy/sampling_vocab.cpp
             skippy/stage_slice_plan.cpp
@@ -99,7 +100,7 @@ set_target_properties(llama PROPERTIES
 )
 
 # exclude non-model sources from unity build
-set_source_files_properties(${LLAMA_CORE_SOURCES} ../include/llama.h unicode.h
+set_source_files_properties(${LLAMA_CORE_SOURCES} ../include/llama.h unicode.h ../common/reasoning-budget.cpp
     PROPERTIES SKIP_UNITY_BUILD_INCLUSION ON)
 
 # Keep model sources out of the unity build as well. The Skippy stage
@@ -110,7 +111,7 @@ set_source_files_properties(${LLAMA_MODELS_SOURCES}
 
 configure_file(llama-version.h.in ${CMAKE_CURRENT_BINARY_DIR}/llama-version.h @ONLY)
 
-target_include_directories(llama PRIVATE . ${CMAKE_CURRENT_BINARY_DIR})
+target_include_directories(llama PRIVATE . ../common ${CMAKE_CURRENT_BINARY_DIR})
 target_include_directories(llama PUBLIC ../include)
 target_compile_features   (llama PRIVATE cxx_std_17) # don't bump
 # Upstream added a models.h PCH for the llama target. The Skippy stage
diff --git a/src/skippy/runtime_types.h b/src/skippy/runtime_types.h
index 7cb885d65..24784dd28 100644
--- a/src/skippy/runtime_types.h
+++ b/src/skippy/runtime_types.h
@@ -145,6 +145,8 @@ struct skippy_session {
     std::vector<int8_t> prefill_logits;
     llama_sampler * sampling_chain = nullptr;
     llama_sampler * grammar_sampler = nullptr;
+    llama_sampler * reasoning_budget_sampler = nullptr;
+    bool grammar_lazy = false;
     bool sampling_backend_enabled = false;
     // A sampler chain may be backend-initialized at most once
     // (llama_sampler_chain_backend_init asserts on reuse). Once a chain is
diff --git a/src/skippy/sampling.cpp b/src/skippy/sampling.cpp
index a900f28ce..99d9731cf 100644
--- a/src/skippy/sampling.cpp
+++ b/src/skippy/sampling.cpp
@@ -3,6 +3,7 @@
 #include "skippy/execution_internal.h"
 #include "skippy/runtime_support.h"
 #include "skippy/runtime_types.h"
+#include "../../common/reasoning-budget.h"
 #include "skippy/sampling_internal.h"
 #include "skippy/speculative_internal.h"
 
@@ -315,8 +316,11 @@ void skippy_clear_chat_sampling(skippy_session * session) {
     }
     llama_sampler_free(session->sampling_chain);
     llama_sampler_free(session->grammar_sampler);
+    llama_sampler_free(session->reasoning_budget_sampler);
     session->sampling_chain = nullptr;
     session->grammar_sampler = nullptr;
+    session->reasoning_budget_sampler = nullptr;
+    session->grammar_lazy = false;
     session->sampling_backend_enabled = false;
     session->sampling_backend_retired = false;
     session->sampling_config = {};
@@ -404,6 +408,7 @@ static bool skippy_sampling_configs_equal(
             lhs.mirostat_entropy != rhs.mirostat_entropy ||
             lhs.mirostat_learning_rate != rhs.mirostat_learning_rate ||
             lhs.sampler_count != rhs.sampler_count ||
+            lhs.reasoning_budget_tokens != rhs.reasoning_budget_tokens ||
             lhs.ignore_eos != rhs.ignore_eos ||
             lhs.dry_sequence_breaker_count != rhs.dry_sequence_breaker_count) {
         return false;
@@ -445,6 +450,7 @@ bool skippy_reuse_plain_chat_sampling(
     if (session == nullptr || sampling == nullptr || metadata_json == nullptr ||
             std::strcmp(metadata_json, "{\"grammar\":\"\"}") != 0 ||
             session->sampling_chain == nullptr || session->grammar_sampler != nullptr ||
+            session->reasoning_budget_sampler != nullptr ||
             !session->sampling_config_valid || session->sampling_config.logit_bias_count != 0 ||
             sampling->logit_bias_count != 0 ||
             !skippy_sampling_configs_equal(session->sampling_config, *sampling)) {
@@ -461,6 +467,7 @@ bool skippy_reuse_plain_chat_sampling(
 bool skippy_reset_reusable_sampling(skippy_session * session) {
     if (session == nullptr || session->sampling_chain == nullptr ||
             session->grammar_sampler != nullptr || !session->sampling_config_valid ||
+            session->reasoning_budget_sampler != nullptr ||
             session->sampling_config.logit_bias_count != 0) {
         return false;
     }
@@ -738,6 +745,7 @@ void skippy_refresh_backend_sampling(
     if (session->ctx != nullptr &&
             session->sampling_chain != nullptr &&
             session->grammar_sampler == nullptr &&
+            session->reasoning_budget_sampler == nullptr &&
             skippy_sampling_is_greedy_equivalent(sampling)) {
         session->sampling_backend_enabled =
                 llama_set_sampler(session->ctx, session->seq_id, session->sampling_chain);
@@ -755,6 +763,7 @@ bool skippy_init_chat_grammar_sampler(
 
     const llama_vocab * vocab = llama_model_get_vocab(session->stage_model->model);
     const bool grammar_lazy = metadata.value("grammar_lazy", false);
+    session->grammar_lazy = grammar_lazy;
     std::vector<std::string> trigger_patterns_storage;
     std::vector<const char *> trigger_patterns;
     std::vector<llama_token> trigger_tokens;
@@ -831,6 +840,81 @@ bool skippy_init_chat_grammar_sampler(
     return true;
 }
 
+bool skippy_init_chat_reasoning_budget_sampler(
+        skippy_session * session,
+        const skippy_sampling_config * sampling,
+        const json & metadata,
+        struct skippy_error ** out_error) {
+    if (session == nullptr || sampling == nullptr) {
+        return true;
+    }
+    const bool finite_budget = sampling->reasoning_budget_tokens >= 0;
+    const bool supports_thinking = metadata.value("supports_thinking", false);
+    const std::string start_tag = metadata.value("thinking_start_tag", std::string());
+    const auto end_tags_json = metadata.find("thinking_end_tags");
+    if (start_tag.empty() || end_tags_json == metadata.end() || !end_tags_json->is_array()) {
+        if (finite_budget && supports_thinking) {
+            skippy_set_error(
+                    out_error,
+                    SKIPPY_STATUS_INVALID_ARGUMENT,
+                    "finite reasoning budget requires thinking start and end tags");
+            return false;
+        }
+        return true;
+    }
+    const bool needs_sampler = finite_budget || session->grammar_lazy;
+    if (!needs_sampler) {
+        return true;
+    }
+    const llama_vocab * vocab = llama_model_get_vocab(session->stage_model->model);
+    const std::vector<llama_token> start_tokens = skippy_tokenize_text(vocab, start_tag, false, true);
+    std::vector<std::vector<llama_token>> end_tokens;
+    for (const auto & value : *end_tags_json) {
+        if (!value.is_string()) {
+            continue;
+        }
+        std::vector<llama_token> tokens = skippy_tokenize_text(vocab, value.get<std::string>(), false, true);
+        if (!tokens.empty()) {
+            end_tokens.push_back(std::move(tokens));
+        }
+    }
+    if (start_tokens.empty() || end_tokens.empty()) {
+        if (finite_budget && supports_thinking) {
+            skippy_set_error(
+                    out_error,
+                    SKIPPY_STATUS_INVALID_ARGUMENT,
+                    "finite reasoning budget requires non-empty tokenized thinking tags");
+            return false;
+        }
+        return true;
+    }
+    const int32_t budget = sampling->reasoning_budget_tokens < 0
+            ? std::numeric_limits<int32_t>::max()
+            : sampling->reasoning_budget_tokens;
+    session->reasoning_budget_sampler = common_reasoning_budget_init(
+            vocab, {start_tokens}, end_tokens, end_tokens.front(), budget);
+    if (session->reasoning_budget_sampler == nullptr) {
+        skippy_set_error(out_error, SKIPPY_STATUS_RUNTIME_ERROR, "failed to initialize reasoning budget sampler");
+        return false;
+    }
+    const std::string generation_prompt = metadata.value("generation_prompt", std::string());
+    for (const llama_token token : skippy_tokenize_text(vocab, generation_prompt, false, true)) {
+        llama_sampler_accept(session->reasoning_budget_sampler, token);
+    }
+    return true;
+}
+
+static bool skippy_chat_grammar_should_apply(const skippy_session * session) {
+    if (session == nullptr || session->grammar_sampler == nullptr) {
+        return false;
+    }
+    if (!session->grammar_lazy || session->reasoning_budget_sampler == nullptr) {
+        return true;
+    }
+    const auto state = common_reasoning_budget_get_state(session->reasoning_budget_sampler);
+    return state == REASONING_BUDGET_IDLE || state == REASONING_BUDGET_DONE;
+}
+
 static void skippy_sync_chat_sampling_history(skippy_session * session) {
     if (session == nullptr || session->sampling_chain == nullptr) {
         return;
@@ -839,7 +923,22 @@ static void skippy_sync_chat_sampling_history(skippy_session * session) {
         const size_t index = session->sampling_accepted_token_count;
         const llama_token token = session->token_history[index];
         llama_sampler_accept(session->sampling_chain, token);
-        if (session->grammar_sampler != nullptr && index >= session->grammar_generated_start) {
+        const bool generated = index >= session->grammar_generated_start;
+        const bool accept_grammar = generated && skippy_chat_grammar_should_apply(session);
+        if (session->reasoning_budget_sampler != nullptr && generated) {
+            llama_sampler_accept(session->reasoning_budget_sampler, token);
+            if (!accept_grammar && session->grammar_sampler != nullptr &&
+                    common_reasoning_budget_get_state(session->reasoning_budget_sampler) == REASONING_BUDGET_DONE) {
+                const std::vector<llama_token> * end_match =
+                        common_reasoning_budget_get_end_match(session->reasoning_budget_sampler);
+                if (end_match != nullptr) {
+                    for (const llama_token end_token : *end_match) {
+                        llama_sampler_accept(session->grammar_sampler, end_token);
+                    }
+                }
+            }
+        }
+        if (accept_grammar) {
             try {
                 llama_sampler_accept(session->grammar_sampler, token);
             } catch (const std::exception & e) {
@@ -884,7 +983,10 @@ static llama_token skippy_chat_sample_token(skippy_session * session, int32_t lo
         -1,
         false,
     };
-    if (session->grammar_sampler != nullptr) {
+    if (session->reasoning_budget_sampler != nullptr) {
+        llama_sampler_apply(session->reasoning_budget_sampler, &cur);
+    }
+    if (skippy_chat_grammar_should_apply(session)) {
         try {
             llama_sampler_apply(session->grammar_sampler, &cur);
         } catch (const std::exception & e) {
@@ -911,6 +1013,7 @@ llama_token skippy_sample_token_ith(
     if (skippy_mtp_greedy_sampling_fastpath_enabled() &&
             !session->sampling_backend_enabled &&
             session->grammar_sampler == nullptr &&
+            session->reasoning_budget_sampler == nullptr &&
             skippy_sampling_is_greedy_equivalent(sampling)) {
         if (session->sampling_chain != nullptr) {
             skippy_clear_chat_sampling(session);
diff --git a/src/skippy/sampling_internal.h b/src/skippy/sampling_internal.h
index 0c4e54724..e48653520 100644
--- a/src/skippy/sampling_internal.h
+++ b/src/skippy/sampling_internal.h
@@ -58,6 +58,11 @@ bool skippy_init_chat_grammar_sampler(
         skippy_session * session,
         const nlohmann::ordered_json & metadata,
         skippy_error ** out_error);
+bool skippy_init_chat_reasoning_budget_sampler(
+        skippy_session * session,
+        const skippy_sampling_config * sampling,
+        const nlohmann::ordered_json & metadata,
+        skippy_error ** out_error);
 llama_token skippy_sample_token_ith(
         skippy_session * session,
         const skippy_sampling_config * sampling,
diff --git a/src/skippy/session.cpp b/src/skippy/session.cpp
index 1fe72b2cc..37b0b9c5b 100644
--- a/src/skippy/session.cpp
+++ b/src/skippy/session.cpp
@@ -315,6 +315,10 @@ enum skippy_status skippy_session_configure_chat_sampling(
             skippy_clear_chat_sampling(session);
             return out_error != nullptr && *out_error != nullptr ? (*out_error)->status : SKIPPY_STATUS_RUNTIME_ERROR;
         }
+        if (!skippy_init_chat_reasoning_budget_sampler(session, sampling, metadata, out_error)) {
+            skippy_clear_chat_sampling(session);
+            return out_error != nullptr && *out_error != nullptr ? (*out_error)->status : SKIPPY_STATUS_RUNTIME_ERROR;
+        }
         skippy_refresh_backend_sampling(session, sampling);
     } catch (const std::exception & e) {
         skippy_clear_chat_sampling(session);
diff --git a/tests/test-reasoning-budget.cpp b/tests/test-reasoning-budget.cpp
index 3bcc77e17..7bed3a0b1 100644
--- a/tests/test-reasoning-budget.cpp
+++ b/tests/test-reasoning-budget.cpp
@@ -1,5 +1,6 @@
 #include "reasoning-budget.h"
 #include "unicode.h"
+#include "common.h"
 
 #include "llama.h"
 #include "ggml.h"
-- 
2.54.0 (Apple Git-157)

