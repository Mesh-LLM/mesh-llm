From 7a83116c3062bd2ff97f452024caa3fd3c60f99e Mon Sep 17 00:00:00 2001
From: James Dumay <jameswdumay@gmail.com>
Date: Sat, 19 Sep 2026 09:52:12 +1000
Subject: [PATCH] fix(skippy): validate package tensors against physical GGUF
 shapes

---
 src/llama-model.cpp                           | 24 +++++++++-
 src/llama-model.h                             |  6 +++
 src/skippy/package_tensor_binding.cpp         | 32 ++++++-------
 .../tests/renamed_multishard_planning.cpp     | 46 +++++++++++++++++++
 4 files changed, 91 insertions(+), 17 deletions(-)

diff --git a/src/llama-model.cpp b/src/llama-model.cpp
index 5a53646fa..39390bb2f 100644
--- a/src/llama-model.cpp
+++ b/src/llama-model.cpp
@@ -1182,6 +1182,8 @@ struct llama_model::impl {
         uint16_t split_no;
         size_t data_offset;
         size_t stored_length;
+        ggml_type type;
+        std::vector<int64_t> dimensions;
     };
 
     std::unordered_map<std::string, tensor_source_location> tensor_source_locations;
@@ -1732,12 +1734,16 @@ bool llama_model_base::load_tensors(llama_model_loader & ml) {
                     weight->idx,
                     weight->offs,
                     ggml_nbytes(weight->tensor),
+                    weight->tensor->type,
+                    std::vector<int64_t>(weight->tensor->ne, weight->tensor->ne + GGML_MAX_DIMS),
                 };
                 const auto inserted = pimpl->tensor_source_locations.emplace(name, location);
                 if (!inserted.second &&
                         (inserted.first->second.split_no != location.split_no ||
                          inserted.first->second.data_offset != location.data_offset ||
-                         inserted.first->second.stored_length != location.stored_length)) {
+                         inserted.first->second.stored_length != location.stored_length ||
+                         inserted.first->second.type != location.type ||
+                         inserted.first->second.dimensions != location.dimensions)) {
                     throw std::runtime_error(format("tensor '%s' has conflicting source locations", name));
                 }
             }
@@ -1961,6 +1967,22 @@ bool llama_model::tensor_source_location(
     return true;
 }
 
+bool llama_model::tensor_source_metadata(
+        const char * name,
+        ggml_type & type,
+        std::vector<int64_t> & dimensions) const {
+    if (name == nullptr) {
+        return false;
+    }
+    const auto found = pimpl->tensor_source_locations.find(name);
+    if (found == pimpl->tensor_source_locations.end()) {
+        return false;
+    }
+    type = found->second.type;
+    dimensions = found->second.dimensions;
+    return true;
+}
+
 std::map<ggml_backend_buffer_type_t, size_t> llama_model::memory_breakdown() const {
     std::map<ggml_backend_buffer_type_t, size_t> ret;
     for (const auto & [ctx, bufs] : pimpl->ctxs_bufs) {
diff --git a/src/llama-model.h b/src/llama-model.h
index cac4c3ce4..39eab7257 100644
--- a/src/llama-model.h
+++ b/src/llama-model.h
@@ -748,6 +748,12 @@ struct llama_model {
             size_t & data_offset,
             size_t & stored_length) const;
 
+    // Physical GGUF metadata, before loader-approved logical reshaping.
+    bool tensor_source_metadata(
+            const char * name,
+            ggml_type & type,
+            std::vector<int64_t> & dimensions) const;
+
     std::map<ggml_backend_buffer_type_t, size_t> memory_breakdown() const;
 
     // total number of parameters in the model
diff --git a/src/skippy/package_tensor_binding.cpp b/src/skippy/package_tensor_binding.cpp
index 1c0ceb180..5fb51f899 100644
--- a/src/skippy/package_tensor_binding.cpp
+++ b/src/skippy/package_tensor_binding.cpp
@@ -18,22 +18,12 @@ bool valid_dimensions(const std::vector<int64_t> & dimensions) {
 }
 
 bool tensor_dimensions_match(
-        const ggml_tensor * tensor,
+        const std::vector<int64_t> & source,
         const std::vector<int64_t> & dimensions) {
-    if (tensor == nullptr) {
-        return false;
-    }
-    // GGUF may pad shapes with trailing ones while ggml drops them from the
-    // effective rank, so compare after trimming trailing ones on both sides.
-    size_t rank = dimensions.size();
-    while (rank > 1 && dimensions[rank - 1] == 1) {
-        --rank;
-    }
-    if (ggml_n_dims(tensor) != static_cast<int>(rank)) {
-        return false;
-    }
-    for (size_t index = 0; index < rank; ++index) {
-        if (tensor->ne[index] != dimensions[index]) {
+    // GGUF permits equivalent ranks padded with trailing ones.
+    for (size_t index = 0; index < GGML_MAX_DIMS; ++index) {
+        const int64_t expected = index < dimensions.size() ? dimensions[index] : 1;
+        if (source[index] != expected) {
             return false;
         }
     }
@@ -94,8 +84,18 @@ bool skippy_bind_package_tensors(
             error = "package tensor locator is absent from the native model: " + input.native_name;
             return false;
         }
+        // Compare the package against the original physical GGUF descriptor,
+        // not the logical shape produced by TENSOR_ALLOW_RESHAPE. Equal element
+        // counts alone would also accept forged package layouts.
+        ggml_type source_type = GGML_TYPE_COUNT;
+        std::vector<int64_t> source_dimensions;
+        if (!model->tensor_source_metadata(input.native_name.c_str(), source_type, source_dimensions) ||
+                source_type != input.type || !tensor_dimensions_match(source_dimensions, input.dimensions)) {
+            error = "package tensor metadata differs from its GGUF source: " + input.native_name;
+            return false;
+        }
         for (const ggml_tensor * tensor : found->second) {
-            if (tensor->type != input.type || !tensor_dimensions_match(tensor, input.dimensions) ||
+            if (tensor->type != input.type || !ggml_is_contiguous(tensor) ||
                     ggml_nbytes(tensor) != input.stored_length) {
                 error = "package tensor metadata differs from the native model: " + input.native_name;
                 return false;
diff --git a/src/skippy/tests/renamed_multishard_planning.cpp b/src/skippy/tests/renamed_multishard_planning.cpp
index 2089c3c12..5ef64d94a 100644
--- a/src/skippy/tests/renamed_multishard_planning.cpp
+++ b/src/skippy/tests/renamed_multishard_planning.cpp
@@ -656,6 +656,48 @@ bool rejects_invalid_bindings(
     return rejects(changed);
 }
 
+// Exercise physical source metadata separately from loader-created logical shapes.
+// The fixture model is already loaded from GGUF, so source dimensions must stay
+// immutable while a native tensor is reshaped as TENSOR_ALLOW_RESHAPE does.
+bool accepts_source_shape_after_native_reshape(
+        llama_model * model,
+        const std::vector<skippy_package_tensor_binding_input> & inputs) {
+    for (const auto & entry : llama_internal_get_tensor_map(model)) {
+        ggml_tensor * tensor = entry.second;
+        if (tensor->type != GGML_TYPE_F32 || ggml_nelements(tensor) < 4 ||
+                ggml_nelements(tensor) % 2 != 0 || !ggml_is_contiguous(tensor)) {
+            continue;
+        }
+        const auto original = *tensor;
+        const int64_t elements = ggml_nelements(tensor);
+        tensor->ne[0] = original.ne[0] == elements ? elements / 2 : elements;
+        tensor->ne[1] = elements / tensor->ne[0];
+        tensor->ne[2] = tensor->ne[3] = 1;
+        tensor->nb[0] = sizeof(float);
+        for (int i = 1; i < GGML_MAX_DIMS; ++i) {
+            tensor->nb[i] = tensor->nb[i - 1] * tensor->ne[i - 1];
+        }
+        std::vector<skippy_parameter_binding> bindings;
+        std::string error;
+        bool ok = skippy_bind_package_tensors(model, inputs, bindings, error);
+        auto forged = inputs;
+        for (auto & input : forged) {
+            if (input.native_name == entry.first) {
+                input.dimensions.assign(tensor->ne, tensor->ne + GGML_MAX_DIMS);
+            }
+        }
+        // Matching the new native shape is not evidence that the package's
+        // physical layout matches GGUF, even when element/byte counts agree.
+        ok = ok && !skippy_bind_package_tensors(model, forged, bindings, error);
+        // Corrupt an active dimension; singleton strides are intentionally ignored.
+        tensor->nb[0] += sizeof(float);
+        ok = ok && !skippy_bind_package_tensors(model, inputs, bindings, error);
+        *tensor = original;
+        return ok;
+    }
+    return false;
+}
+
 std::string plan_string(const skippy_stage_plan * plan, skippy_stage_plan_string_ref_v1 reference) {
     const char * data = nullptr;
     size_t length = 0;
@@ -987,6 +1029,10 @@ int run_proof(const char * source_path, const char * output_directory, bool expe
         std::fprintf(stderr, "invalid package tensor binding was accepted\n");
         return 6;
     }
+    if (!accepts_source_shape_after_native_reshape(model.get(), inventory.tensors)) {
+        std::fprintf(stderr, "physical/logical package tensor shape regression failed\n");
+        return 6;
+    }
     std::vector<std::string> prefill;
     std::vector<std::string> batched;
     std::vector<std::string> decode;
-- 
2.54.0 (Apple Git-157)

